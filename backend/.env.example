SERPAPI_KEY=
OPENAI_API_KEY=
MOCK_MODE=false

# MOCK_MODE=true uses fake data only. Set to false to use SerpAPI + OpenAI.

# For URL validation (screenshot + vision): pip install playwright && playwright install chromium
