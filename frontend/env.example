# Backend API URL (used by frontend to call the API).
# Next.js bakes NEXT_PUBLIC_* in at build time, so one value per build:
#
#   Local:     keep .env.local with NEXT_PUBLIC_API_URL=http://localhost:8001
#   Deployed:  in Vercel set env var NEXT_PUBLIC_API_URL=https://agentic-commerce-lh5f.vercel.app
#
# Then local dev uses localhost, deployed app uses the real URL. Do not put both in one file.
NEXT_PUBLIC_API_URL=http://localhost:8001

# Frontend runs at:
#   Local:   http://localhost:3001
#   Deployed: your Vercel URL (e.g. https://your-app.vercel.app)

# Supabase (auth, liked products, chat history)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
